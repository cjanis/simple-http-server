[uwsgi]
module = server

master = true
processes = 5

# for wsgi socket
# socket = simple-http-server.socket
# chmod-socket = 666
# vacuum = true

# for testing only without nginx
socket = 127.0.0.1:8180
protocol = http

# that's how we call app in server.py
callable = app

die-on-term = true
